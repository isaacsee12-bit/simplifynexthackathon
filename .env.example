# Backend only. Never expose this secret through a VITE_ variable.
GEMINI_API_KEY=
# Model availability varies by account; configure an available model if needed.
GEMINI_MODEL=gemini-3.5-flash
# Optional comma-separated browser origins for a separately hosted frontend.
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
